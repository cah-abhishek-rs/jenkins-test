pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                echo "hello I am here"
                sh 'aws'
                sh 'kubectl'
                sh 'helm'
                sh 'terraform'
            }
        }
    }
}